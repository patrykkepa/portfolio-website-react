import React, { Component } from 'react';

import '../css/components/footer.css';

const Footer = (props) => {
    return (
        <section>
            This is a footer
        </section>
    )
}

export default Footer;