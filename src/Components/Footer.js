import React, { Component } from 'react';

import '../css/components/footer.css';

const Footer = (props) => {
    return (
        <footer>
            This is a footer
        </footer>
    )
}

export default Footer;